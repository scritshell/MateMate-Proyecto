<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para item de apertura en RecyclerView (lista vertical) -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <!-- Layout horizontal para organizar contenido -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- Emoji representativo de la apertura -->
        <TextView
            android:id="@+id/tvEmojiApertura"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="♟️"
            android:textSize="24sp"
            android:layout_marginEnd="16dp" />

        <!-- Contenedor vertical para nombre y descripción -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Nombre de la apertura -->
            <TextView
                android:id="@+id/tvNombreApertura"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Apertura Italiana"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!-- Movimientos principales de la apertura -->
            <TextView
                android:id="@+id/tvDescripcionApertura"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="1.e4 e5 2.Cf3 Cc6 3.Ac4"
                android:textSize="14sp"
                android:textColor="#666" />

        </LinearLayout>

        <!-- Icono de flecha para indicar navegación -->
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@android:drawable/ic_media_next"
            app:tint="#666" />

    </LinearLayout>

</androidx.cardview.widget.CardView>